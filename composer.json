{
    "require": {
        "php": ">=8.0",
        "monolog/monolog": "^2.0 || ^3.0"
    },
    "autoload": {
        "psr-4": {
            "WhatsappBridge\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
